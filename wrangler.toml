name = "orange-cat-418"
main = "src/worker.js"
compatibility_date = "2024-09-23"

# 将 HTML 文件作为文本模块导入 Worker
rules = [
  { type = "Text", globs = ["**/*.html"], fallthrough = false }
]

# 部署后任意路径访问都会返回 418 维护页
# 部署命令: npx wrangler deploy
# 本地预览: npx wrangler dev

[observability]
enabled = true
